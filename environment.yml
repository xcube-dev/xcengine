name: xcengine
channels:
  - conda-forge
dependencies:
  # Python
  - python >=3.10
  # Required
  - click
  - docker-py
  - ipython  # Used by nbconvert to transform IPython syntax to pure Python
  - nbformat
  - nbconvert
  - xarray
  - xcube
